# SPEC M1c — モジュレーションマトリクス（6スロット）とマクロ（2本）

対象: `core/` のみ。前提: M1b-4 完了（パラメータ 0〜54、engine version 5）。
`shells/` は触らない（AU と Web は M1b-2 の仕組みで自動的に新パラメータを拾う。動作確認は Claude が行う）。

## 何を作るか

いま LFO の送り先は「カットオフ・ピッチ・音量」の3本に直結しているだけ。
**どの信号源を、どのパラメータへ、どれだけ送るか**を自由に組める仕組みにする。これがシンセの表現力の中心。

あわせて**マクロを2本**用意する。1つのつまみで複数のパラメータを同時に動かすためのもので、
**片方は random-scale-keys の「緊張度T」を受ける口**として使う（外部から曲の緊張度を送ると音色が変わる）。

## 追加パラメータ（55〜74。`kParamCount` は 75、`synth_engine_version()` は 6）

**6スロット × 3項目**（スロット `n` = 0〜5 について、`55 + n*3` から）:

| 相対 | 名前 | 範囲 | 既定 | 説明 |
|---|---|---|---|---|
| +0 | `modSlot<n>Source` | 0..7 int | **0** | 信号源。0=なし |
| +1 | `modSlot<n>Dest` | 0..13 int | 0 | 送り先。0=なし |
| +2 | `modSlot<n>Amount` | -1..1 | 0 | 深さ（双極性） |

`identifier` は `modSlot0Source` のように 0 始まりの通し番号で付ける（表示名は `Mod 1 Source` のように 1 始まり）。

| id | 名前 | 範囲 | 既定 | 説明 |
|---|---|---|---|---|
| 73 | `macro1` | 0..1 | 0 | マクロ1 |
| 74 | `macro2` | 0..1 | 0 | マクロ2（**random-scale-keys の緊張度T を受ける口**という運用） |

## 信号源（Source）

| 値 | 名前 | 値の範囲 | 単位系 | 備考 |
|---|---|---|---|---|
| 0 | なし | — | — | スロット無効 |
| 1 | LFO | -1..1 | 双極性 | 既存の LFO をそのまま使う（フリーラン／リトリガーの設定に従う） |
| 2 | アンプEG | 0..1 | 単極性 | ボイスごと |
| 3 | フィルタEG | 0..1 | 単極性 | ボイスごと |
| 4 | ベロシティ | 0..1 | 単極性 | ノートオン時の値を保持 |
| 5 | ノート位置 | -1..1 | 双極性 | `(midiNote - 60) / 60` を -1..1 にクランプ。C4 で 0 |
| 6 | マクロ1 | 0..1 | 単極性 | 全ボイス共通 |
| 7 | マクロ2 | 0..1 | 単極性 | 全ボイス共通 |

## 送り先（Destination）と換算

`amount` は -1..1。**下の「full」の値を掛けたものが、そのパラメータへの加算量**になる。

| 値 | 送り先 | full（amount=1 のときの変化量） | 加算のしかた |
|---|---|---|---|
| 0 | なし | — | — |
| 1 | Osc A Level | ±4.0 | 線形加算、結果を 0..4 にクランプ |
| 2 | Osc B Level | ±4.0 | 同上 |
| 3 | Osc A Morph | ±1.0 | 線形加算、0..1 にクランプ |
| 4 | Osc B Morph | ±1.0 | 同上 |
| 5 | FM B→A | ±1.0 | 線形加算、0..1 にクランプ |
| 6 | Sub Level | ±4.0 | 線形加算、0..4 にクランプ |
| 7 | Noise Level | ±4.0 | 同上 |
| 8 | Filter Cutoff | ±8 オクターブ | **既存のカットオフ合成式の `cents` に加算**（`lfoToCutoff` と合算） |
| 9 | Filter Resonance | ±1.0 | 線形加算、0..1 にクランプ |
| 10 | Pitch（全オシレーター） | ±1200 cent | **既存の `lfoToPitch` と合算**して `2^(cents/1200)` を周波数へ |
| 11 | Osc A Detune | ±50 cent | 線形加算、0..50 にクランプ |
| 12 | LFO Rate | ±8 オクターブ | `rate * 2^(oct)`、結果を 0.01..40 Hz にクランプ |
| 13 | Amp（ボイス出力） | ±1.0 | **乗算**。`gain *= clamp(1 + value, 0, 2)`。`lfoToAmp` の後に掛ける |

- **同じ送り先に複数スロットが向いていたら、すべて合算する**（クランプは合算後に1回だけ）。
- 単極性の信号源（0..1）はそのまま、双極性（-1..1）もそのまま `amount` に掛ける。
  つまり `寄与 = source値 * amount * full`。
- **評価はサンプルごと・ボイスごと**。マクロとフリーランLFOは全ボイス共通の値を使う。
- 送り先 12（LFO Rate）だけは全ボイス共通の値として扱う（ボイスごとにLFO速度が変わると破綻するため、
  **ボイス0番の評価結果を使う**。この決めごとを docs に明記すること）。

## マクロをイベントで動かす

いま `SYNTH_EV_MACRO`（kind=4）は順序に参加するだけで音に影響しない。これを**マクロの入力口**にする。

- `event.id` = 0 → `macro1`、`1` → `macro2`。それ以外は無視（戻り値の無視件数に数える）。
- `event.a` = 値（0..1 にクランプ）。**指定した offset のサンプルから反映**する。
- ★マクロは **一次スムーサ（時定数 5 ms）**を通す（カットオフと同じ方式）。時定数なので 5 ms で 63%、15 ms で 95% に達する。`synth_reset` でスナップする。
  そうしないと外部から急に値を変えたときにジッパーノイズが出る。
- `synth_set_param(73/74, v)` でも同じ値を設定できる（AU のノブから触るため）。

## 後方互換（最重要）

- 全スロットの `Source` 既定が 0（なし）、マクロ既定 0 なので、**既存の出力とビット一致**すること。
  基準ファイルは実装前に取得する（下記）。

## テスト（42〜48 を追加。既存41項目は壊さない）

42. **バイパスのビット一致**: 全スロット無効で `presets/m0_saw.txt`・`presets/m1_unison_saw.txt`・
    `presets/m1b_filter_sweep.txt` の3つが**変更前とビット一致**。
43. **信号源7種**: 各 Source を「Osc A Level」へ amount=1 で送り、期待どおりに効くことを確認。
    - ベロシティ: velocity 0.25 と 1.0 で鳴らし、RMS の比が **3.5〜4.5 倍**
    - ノート位置: C1 と C7 で符号が逆になる
    - マクロ1: 値 0 と 1 で RMS が変わる
    - アンプEG／フィルタEG／LFO: 出力が時間変化する（無変調時との差が RMS で 1 dB 以上）
44. **送り先13種**: 各 Destination へ LFO を amount=1 で送り、**無変調時と RMS もしくはスペクトル重心が
    有意に変わる**（差 1 dB 以上、または重心 5% 以上）。13個すべてについて数値を出す。
45. **合算**: 同じ送り先（Filter Cutoff）へ2スロットから同じ量を送ると、1スロットのときより
    カットオフの変化幅が**おおよそ2倍**（±20%）になる。
46. **マクロのイベント**: `SYNTH_EV_MACRO` を frame 24000 で 0→1 に変え、`macro1`→`Osc A Level` を
    amount=1 で結線したとき、**24000 の前後で RMS が変わる**。
    ★スムーサの「5 ms」は**時定数**であって整定時間ではない。実測すると 5 ms で 63%、15 ms で 95% 到達
    （2026-09-05 Claude 確認）。したがって判定は「24000 の直後は元の値に近く、5 ms 後には目標の 60% を超えている」とする。
47. **決定論とブロック不変性**: 6スロット全部を別々の source/dest で埋めた状態で、
    block 1／7／64／128／511 でビット一致、reset→レンダー2回でビット一致。
48. **性能**: 6スロット有効・LP24・LFO・16音×ユニゾン4 で 1000ブロック。
    平均と p99 を出し、**p99 が期限（2667 µs）の 50% 未満**。

## プリセットを1つ追加

- `presets/m1c_macro_morph.txt` — `macro1` を「Osc A Morph」と「Filter Cutoff」の両方へ送り、
  **1つのつまみで音が開いていく**プリセット。マクロの使い所が分かるもの。

## 完了条件

- `make test` が **48/48 PASS**。
- `make core-freestanding-check` PASS、`make wasm WASM_CLANG=/opt/homebrew/opt/llvm/bin/clang` 成功。
- `README.md` の「いまできること」表と `docs_BUILD.md`（パラメータ一覧・テスト一覧・未決事項）を更新。
- テスト43・44・45・48の実測値を報告する。

## やらないこと

- スロット数の可変化（6固定）。モジュレーションのカーブ／量子化。MIDI CC の直接受け（マクロ経由で行う）。
- `shells/` `tools/` `design/` の変更。ファイルの削除・移動。
