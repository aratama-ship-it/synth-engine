# SPEC M1b-3 — エンベロープのカーブ（ディケイ／リリースの形）

対象: `core/` のみ。前提: M1b 完了（パラメータ 0〜52、engine version 3）。

## なぜ作るか（本人の指摘と実測）

2026-09-05、本人がフィルタスイープの試聴で「**スイープというより、アタックで一気にフィルターがかかる感じ**」と指摘。
Claude が実測したところ、`filterEgDecay = 0.9秒` に設定しているのに、
**明るさの変化の 50% が 0.03 秒、80% が 0.12 秒で終わっていた**（減衰時間の 3% と 13% の時点）。

原因は現在のディケイが **1極の指数**（`coef = 2^(-8/samples)`）で、動きが頭に極端に偏るため。
指数カーブ自体はアナログシンセの標準的な挙動なので**間違いではない**が、**カーブを選べないのが問題**。
Serum を含む現代のシンセはエンベロープの各区間にカーブ調整を持つ。それを足す。

## 追加パラメータ（53〜54。`kParamCount` は 55、`synth_engine_version()` は 4）

| id | 名前 | 範囲 | 既定 | 説明 |
|---|---|---|---|---|
| 53 | ampEgCurve | 0..1 | **0** | アンプEGのディケイ／リリースの形。0=現行の指数、1=直線 |
| 54 | filterEgCurve | 0..1 | **0** | フィルタEGのディケイ／リリースの形。同上 |

- **既定 0 は現行と完全に同じ挙動**（＝ゴールデンとビット一致）。
- アタック区間は現状すでに直線なので変更しない。

## 実装

**`curve == 0` のときは既存のコードパスをそのまま通す**（ビット一致のため。分岐で分ける）。

`curve > 0` のときは、区間の進行度 `p`（0→1、1サンプルごとに `1/samples` 進む）を持つ方式に切り替える:

```
u = 1 - p                                  区間の残り（1→0）
expShape(u) = (2^(-8*(1-u)) - 2^-8) / (1 - 2^-8)     現行の指数を、終端でちょうど0になるよう正規化
shape(u, c) = (1 - c) * expShape(u) + c * u          c=0 で指数、c=1 で直線

decay 区間 :  envelope = sustain + (1 - sustain) * shape(u, c)
              p >= 1 になったら envelope = sustain、sustain 区間へ
release 区間: envelope = releaseStartLevel * shape(u, c)
              p >= 1 になったら envelope = 0、ボイス解放
```

- `releaseStartLevel` はノートオフ時のエンベロープ値（ボイスに保持する）。
- `samples <= 1` のときは従来どおり即座に目標値へ。
- ★`curve > 0` の経路では**区間の長さが `decay`／`release` 秒ちょうど**になる（現行の指数は漸近で、
  `2^-8`＝99.6% に達した時点を終わりとみなしていた）。この違いは docs に明記する。

## テスト（35〜38 を追加。既存 34項目は壊さない）

35. **カーブ0のビット一致**: `ampEgCurve=0`・`filterEgCurve=0` で、`presets/m0_saw.txt` と
    `presets/m1_unison_saw.txt` の出力が**変更前とビット一致**（実装前にゴールデンを取ること）。
36. **直線カーブの均等性**: `filterEgCurve=1`・`filterEnvAmount=4`・`decay=1.0s`・`sustain=0` で、
    フィルタEGの値をディケイ区間の 25%／50%／75% の時刻で取り出し、
    それぞれ **0.75／0.50／0.25 に対して誤差 ±0.03 以内**であること（＝時間に対して直線）。
37. **カーブの単調性**: `curve = 0, 0.5, 1` で「エンベロープが 0.5 まで落ちる時刻」を測り、
    **curve が大きいほど遅くなる**（0 < 0.5 < 1 の順に単調増加）。
38. **全域の健全性**: curve を 0／0.25／0.5／0.75／1、decay と release を 0／0.01／1／20 秒で組み合わせて
    各 0.3 秒レンダーし、NaN/Inf 0、`|sample| <= 8`、**ボイスが必ず解放される**（リリース後に発音が残らない）。

## プリセットの更新

- `presets/m1b_filter_sweep.txt` の `54`（filterEgCurve）を **0.8** にする（本人の指摘への直接の答え）。
- 新規 `presets/m1b_sweep_linear.txt` — 直線カーブ（curve=1.0）の比較用。他は `m1b_filter_sweep.txt` と同じ値にする。

## 完了条件

- `make test` が **38/38 PASS**。テスト35（ビット一致）を必ず含む。
- `make core-freestanding-check` PASS、`make wasm WASM_CLANG=/opt/homebrew/opt/llvm/bin/clang` 成功。
- 上の2プリセットが `nan_count=0` でレンダーできる。
- `docs_BUILD.md` のパラメータ一覧・テスト一覧・未決事項を更新し、
  「curve>0 では区間長が指定秒ちょうどになる」ことを明記する。
- テスト36・37の実測値を報告する。

## やらないこと

- アタック区間のカーブ。区間ごとに別々のカーブ値を持つこと（1つのEGにつき1つの値でよい）。
- `shells/` `tools/` `design/` の変更。ファイルの削除・移動。
